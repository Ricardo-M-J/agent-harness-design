# 系统架构说明

## 整体架构

本系统基于OpenCode Harness框架构建，采用Primary Agent + Subagent的两级架构：

```
┌─────────────────────────────────────────────────────┐
│                   OpenCode Harness                   │
│                                                      │
│  ┌──────────┐   ┌───────────┐   ┌──────────┐       │
│  │ Planner  │──▶│ Designer  │──▶│  Critic  │──┐    │
│  │  Agent   │   │   Agent   │   │  Agent   │  │    │
│  └──────────┘   └───────────┘   └──────────┘  │    │
│       Subagent      Subagent       Subagent    │    │
│                                                │    │
│  ┌─────────────────────────────────────────────┐│    │
│  │  Primary Agent (build) - 编排者             ││    │
│  │  读取 AGENTS.md 作为系统提示               │◀┘    │
│  └─────────────────────────────────────────────┘     │
│                                                      │
│  ┌─────────────────────────────────────────────┐    │
│  │  Skills + Custom Tools                       │    │
│  │  brand-design | copywriting | design-spec    │    │
│  │  design-review | text-to-image (Mock)        │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

## 自迭代机制原理

### 为什么需要自迭代？

传统单次pipeline的问题：
- Designer一次生成的结果质量不稳定
- 没有质量反馈机制
- 无法保证设计达到可交付标准

### 自迭代如何工作？

```
第1轮：Designer生成 → Critic评分62 → 需要迭代
第2轮：Designer增量修改 → Critic评分78 → 继续迭代（+16）
第3轮：Designer微调 → Critic评分82 → 达标停止（+4）
```

### 关键设计决策

#### 1. 差异化反馈（非模糊评价）
Critic不只说"不够好"，而是：
- 每个维度独立评分（0-100）
- 只对低于70分的维度给出改进建议
- 改进建议必须具体可操作

#### 2. 增量修改（非全部重来）
Designer收到反馈后：
- 只修改Critic指出的低分维度
- 保留高分维度不变
- 避免推翻重来导致的质量波动

#### 3. 收敛检测（非无限循环）
三个停止条件（满足任一）：
- 总评分 >= 80（质量达标）
- 评分增量 < 5（收益递减）
- 迭代轮次 >= 5（成本控制）

### 与OpenDesign的对比

| 维度 | OpenDesign | 本系统 |
|------|-----------|--------|
| 评审 | Agent自评 | 独立Critic Agent |
| 反馈 | 模糊建议 | 结构化评分+具体指令 |
| 迭代 | 无显式循环 | 有控制的迭代循环 |
| 改进 | 可能全部重来 | 增量修改 |
| 停止 | 无 | 收敛检测 |

## Agent职责

### Planner Agent
- **输入**：用户的设计需求（自然语言）
- **处理**：搜索信息 → 分析需求 → 制定方案 → 拆解任务
- **输出**：设计方案文档（品牌定位+色彩方向+任务清单）
- **工具**：websearch、webfetch、write

### Designer Agent
- **输入**：设计方案 +（可选）Critic改进指令
- **处理**：创意文案 → 设计规范 → 文生图
- **输出**：文案+规范+图像文件
- **工具**：text-to-image、write、skill
- **迭代模式**：收到反馈后只修改低分维度

### Critic Agent
- **输入**：设计方案 + 设计结果
- **处理**：五维评分 → 改进建议 → 收敛判断
- **输出**：评审报告（评分+建议+是否迭代）
- **约束**：只读，不能修改任何文件