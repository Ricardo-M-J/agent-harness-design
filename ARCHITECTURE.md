# 系统架构说明 — "薄 Harness + 厚 Skill"

## 设计哲学

本系统的核心竞争力不在 Agent 编排代码，而在**可移植的设计知识编码 (SKILL.md)**。

```
┌──────────────────────────────────────────────────────────┐
│  OpenCode Harness (薄 — 只做路由和编排)                    │
│                                                          │
│  opencode.json (30行)     AGENTS.md (100行)              │
│  路由配置                  编排逻辑                        │
│                                                          │
│  ┌──────────┐   ┌───────────┐   ┌──────────┐           │
│  │ Planner  │──▶│ Designer  │──▶│  Critic  │──┐        │
│  │  Agent   │   │   Agent   │   │  Agent   │  │        │
│  │ (80行)   │   │  (100行)  │   │ (100行)  │  │        │
│  └──────────┘   └─────┬─────┘   └──────────┘  │        │
│                       │                       │        │
│  ┌────────────────────┼───────────────────────┘        │
│  │            厚 Skill 层（设计知识 = 护城河）            │
│  │                                                      │
│  │  brand-design (400行)    visual-output (200行)       │
│  │  ├ 反套路清单            ├ HTML页面6步工作流          │
│  │  ├ oklch色彩系统         ├ 设计质量标准               │
│  │  ├ 6组色彩×字体配对      ├ 面试展示模式               │
│  │  └ 6步品牌设计工作流     └ Anti-AI-Cliché约束         │
│  │                                                      │
│  │  design-spec (200行)     copywriting (150行)         │
│  │  ├ 设计Token系统         ├ 标语体系模板               │
│  │  ├ CSS/Tailwind/JSON     ├ 品牌故事结构               │
│  │  └ 多格式导出            └ 文案禁令                   │
│  │                                                      │
│  │  design-review (150行)                               │
│  │  ├ 五维自评审标准                                     │
│  │  ├ AI套路检测清单                                    │
│  │  └ P0/P1/P2质量门禁                                  │
│  │                                                      │
│  │  所有 SKILL.md 可移植到 Claude Code/Cursor/Codex      │
│  └──────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Custom Tools                                         │ │
│  │  text-to-image.ts — Pollinations.ai 免费文生图        │ │
│  └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

## 五维自评审体系

借鉴 Open Design 的五维自评审（Five-Dimensional Self-Criticism），每一轮设计完成后由 Critic Agent 从 5 个维度独立评分：

| 维度 | 英文 | 评估焦点 |
|------|------|----------|
| **哲学性** | Philosophy | 设计理念自洽、品牌叙事深度 |
| **层级性** | Hierarchy | 视觉层级、信息架构清晰度 |
| **执行度** | Execution | 细节打磨、工艺水平 |
| **特异性** | Specificity | 差异化、辨识度 |
| **克制感** | Restraint | 避免过度装饰 |

配合 **P0/P1/P2 三级质量门禁** 和 **AI 套路检测清单**，形成三层递进的质量保障。

## 自迭代机制

### 差异化反馈
- Critic 对五个维度独立评分（1-5 分制）
- 转换为加权百分制
- AI 套路检测扣分
- P0 门禁不通过 → 强制上限 60 分 → 强制迭代

### 增量修改
- Designer 只修改评分 < 3/5 的维度
- 保留评分 ≥ 4/5 的维度不变
- 明确标注改动清单

### 收敛检测
三个停止条件（满足任一）：
1. 总评分 ≥ 80（质量达标）
2. 评分增量 < 5（收益递减）
3. 迭代轮次 ≥ 5（成本控制）

## Agent 职责

### Planner Agent
- **设计意图捕获**：6 维度分析（表面层/受众/语调/品牌上下文/规模/约束）
- **3 个差异化色彩方向**：深色系/暖色系/亮色系
- **P0/P1/P2 任务清单**
- **目标受众分层分析**

### Designer Agent
- **6 步设计工作流**：理解→收集→声明→草稿→构建→自检
- **设计 Token 先于设计** (Spec First)
- **反套路自检**：每轮输出前过 Anti-AI-Cliché 清单
- **占位符哲学**：不确定的用 `[placeholder]`，不造假

### Critic Agent
- **五维评分**：每维度 5 级明确标准
- **AI 套路检测**：7 项检测 + 对应扣分
- **P0/P1/P2 门禁**
- **结构化改进指令**：具体可操作，非模糊评价

## 与基线方案的对比

| 维度 | 基线方案 (v0.2.0) | 本系统 (v1.0) |
|------|------------------|---------------|
| Agent prompt | ~15 行 | ~80-100 行 |
| Skill 深度 | ~5 行/个 | 100-400 行/个 |
| 评审体系 | 4 维简单评分 | 五维自评审 + AI 套路检测 + P0/P1/P2 |
| 色彩系统 | HEX 色值 | oklch 感知均匀 + 6 组精选配对 |
| 反套路 | 无 | Anti-AI-Cliché Blocklist |
| 文生图 | Mock (不存在) | Pollinations.ai 免费 API |
| 输出形式 | 纯 Markdown | Markdown + HTML 品牌页面 |
| 设计 Token | 无 | 完整 Token 系统 + 多格式导出 |
| 可移植性 | 绑定 OpenCode | SKILL.md 跨框架可移植 |
